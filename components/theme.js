import { createMuiTheme } from '@material-ui/core/styles';
import {blue} from '@material-ui/core/colors';

const theme = createMuiTheme({
  backgroundColor: '#672222',
  palette: {
    primary: {main:"#1A2840"},
    secondary: {main:"#73b33f"}
  },
});

export default theme
